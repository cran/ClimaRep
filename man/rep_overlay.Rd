% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rep_overlay.R
\name{rep_overlay}
\alias{rep_overlay}
\title{Overlay ClimaRep classifications into cumulative counts (climate redundancy)}
\usage{
rep_overlay(
  folder_path,
  output_dir = file.path(tempdir(), "ClimaRep_overlay"),
  change = FALSE
)
}
\arguments{
\item{folder_path}{\code{character}. The path to the directory containing the classification rasters (\code{.tif}) generated by \code{ClimaRep::mh_rep()} or \code{ClimaRep::mh_rep_ch()}. These rasters should primarily
contain the categories: \code{1} (Stable/Analogue), \code{2} (Lost), and \code{3} (Novel).
Category \code{0} (Non-analogue) will be ignored for the RGB output.}

\item{output_dir}{\code{character}. Path to the directory where the output file will be saved.}

\item{change}{\code{logical}. If \code{TRUE}, processes inputs as outputs from \code{ClimaRep::mh_rep_ch()} (Lost/Stable/Novel three-band RGB output). If \code{FALSE} (default), processes inputs as outputs from \code{ClimaRep::mh_rep()}.}
}
\value{
Writes the following outputs within the directory specified by \code{output_dir}:
When \code{ClimaRep::mh_rep()} results are used, the output layers consistently represent counts for \strong{Analogue} categories across all input rasters.
When \code{ClimaRep::mh_rep_ch()} results are used, the output layers consistently represent counts for \strong{Lost} (Red), \strong{Stable} (Green), and \strong{Novel} (Blue) categories across all input rasters. Designed for direct RGB plotting.
\itemize{
\item A multi-layered \code{SpatRaster} (\code{ClimaRep_overlay.tif}) for RGB visualization.
\item Individual \code{.tif} files for each band (Lost, Stable, Novel) in \verb{individual_bands/} subdirectory.
\item \verb{Additional note}: The \code{ClimaRep::mh_rep()} function analyzes a single period. When its output is used, analogues cells are all categorized as Stable (value 1), while other categories (Lost and Novel) have a value of zero.
}
}
\description{
Combines multiple single-layer rasters (\code{tif}), outputs from \code{ClimaRep::mh_rep()} or \code{ClimaRep::mh_rep_ch()} for different input polygons, into a multi-layered \code{SpatRaster}, depending on the type of input.

This function handles inputs from both \code{ClimaRep::mh_rep()} (which primarily contains \strong{Analogue} cells) and \code{ClimaRep::mh_rep_ch()} (which includes \strong{Stable}, \strong{Lost}, and \strong{Novel} cells). The output layers consistently represent counts of each input.
When \code{mh_rep()} outputs are aggregated, the resulting single-layer count is the climate redundancy: the number of reference polygons for which each cell is a climate analogue.

\itemize{
\item \code{change = FALSE} (default): inputs are treated as outputs of
\code{ClimaRep::mh_rep()}. Only \strong{Analogue} cells (value \code{1}) are counted,
producing a single-layer cumulative count raster (climate redundancy).
\item \code{change = TRUE}: inputs are treated as outputs of \code{ClimaRep::mh_rep_ch()}.
\strong{Lost} (\code{2}), \strong{Stable} (\code{1}) and \strong{Novel} (\code{3}) cells are counted
separately, producing a three-layer \code{SpatRaster} ready for RGB plotting.
}
}
\details{
This function aggregates the per-polygon binary or categorical rasters produced by \code{\link{mh_rep}} or \code{\link{mh_rep_ch}} into cumulative count layers across
all input polygons. It is designed as a post-processing step to summarise where, and how often, a given category occurs in space when many polygons have been analysed.

The behaviour is controlled by the \code{change} argument:
\itemize{
\item \code{change = FALSE} (default): inputs are treated as outputs of \code{ClimaRep::mh_rep()}. For each input raster, a binary layer is created where
cells with value \code{1} (Analogue) are set to \code{1} and all other values to \code{0}. These layers are summed across all rasters to produce a single-layer
cumulative count of how many polygons classify each cell as a climate analogue (climate redundancy).
\item \code{change = TRUE}: inputs are treated as outputs of \code{ClimaRep::mh_rep_ch()}. Three independent cumulative count layers are
produced, one for each change category: cells coded \code{2} (Lost), \code{1} (Stable), and \code{3} (Novel). Cells coded \code{0} (non-analogue) are ignored.
The three count layers are stacked in the fixed order Lost (Red), Stable (Green), Novel (Blue), so the resulting \code{SpatRaster} is ready for direct RGB
visualization with \code{terra::plotRGB()}, where colour mixtures intuitively reflect the spatial agreement among change types.
}

Output structure within \code{output_dir}:
\itemize{
\item \code{ClimaRep_overlay.tif}: the final multi-layer (or single-layer) \code{SpatRaster} of cumulative counts.
\item \verb{individual_bands/}: only created when \code{change = TRUE}. Contains one \code{.tif} per RGB channel \code{lost_count_R.tif}, \code{stable_count_G.tif}, \code{novel_count_B.tif} for users who prefer to handle each band separately.
}
}
\examples{
ClimaRep_overlay <- ClimaRep::rep_overlay(folder_path = system.file("extdata",
                                                                    package = "ClimaRep"),
                                         output_dir = file.path(tempdir(), "rep_overlay_output"),
                                         change = FALSE)
terra::plot(ClimaRep_overlay)
}
